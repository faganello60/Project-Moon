version: '3.8'

services:
    backend:
      build: .
      expose:
        - "8000"
      restart: unless-stopped
  
    frontend:
      build: ./web
      ports:
        - "8080:80"
      depends_on:
        - backend
      restart: unless-stopped
  